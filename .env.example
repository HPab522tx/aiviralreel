# Public, client-safe values only. Server secrets must NOT use VITE_ prefix.
VITE_API_BASE_URL=/api
VITE_WS_BASE_URL=
VITE_USE_MOCK_API=true

# Server-only (read inside createServerFn .handler())
# OPENAI_API_KEY=
# SUPABASE_URL=
# SUPABASE_SERVICE_ROLE_KEY=
# STRIPE_SECRET_KEY=
# FFMPEG_BIN=ffmpeg
